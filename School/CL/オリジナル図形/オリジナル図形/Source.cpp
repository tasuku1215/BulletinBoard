#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int main(void)
{
	int tate, yoko,yoko2, hen,val,val2;
	rewind(stdin);
	scanf("%d", &hen);
	for (tate=1;;tate++)
	{
		printf("‚Ë");
		printf("%d",tate);
	}
	
	rewind(stdin);
	getchar();
	return 0;
}